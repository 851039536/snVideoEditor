# 2026-06-30

## EncryptView.vue 审查与修复（不涉及加密逻辑）
- **新增** `views/Encrypt/types/index.ts` — 移入 `CryptoEntry` 接口（原则1：功能模块化）
- **新增** `assets/styles/_encrypt.scss` — 提取 `code` 样式（原则6：样式分离）
- **删除** `.input-field` 死 CSS（手动密码输入移除后遗留，记忆 ID 66922502）
- **删除** `CryptoEntry` 内联接口声明
- lint: 仅 1 个 pre-existing `@use` CSS 误报，零新增错误

## Player 模块逻辑审查与修复（共 11 项）

### 内存泄露修复
1. `onUnmounted` → `async` + `await cleanupAllTemps()`，确保组件销毁前临时文件被删除
2. `removeFile` → `async` + `await cleanupTemp()`，先删文件再从列表移除

### 逻辑漏洞修复
3. IIFE init `tempDir` 改为 `tempDirReady` Promise + try-catch，`decryptAndPlay` 内 `await tempDirReady` 确保就绪
4. `.enc` 检测改为 `p.toLowerCase().endsWith('.enc')` 替代 `split('.').pop()`
5. `loadMeta` 增加 `entry.isEncrypted && !entry.tempPath` 防御性守卫
6. 新增 `decrypting` 并发锁，防止快速切换触发多重解密
7. 错误横幅增加 X 关闭按钮

### 冗余消除
8. 移除 `tempPaths: Map<string,string>` — 改为 computed `tempCount` 从 files 派生
9. `cleanupAllTemps` 改为直接遍历 files 而非 Map
10. 移除 `decryptWithDefaultKey()` 一层包装，内联为 `decryptAndPlay(file, DEFAULT_ENCRYPT_KEY)`
11. 模板 `tempPaths.size` → `tempCount`

lint: 零新增错误，1 个 pre-existing `@use` 误报

## Player 模块持久化扩展（5 项）

遵循项目现有 Pinia + localStorage + watch 模式，为 Player 播放器添加完整持久化。

### 修改文件
1. **types/index.ts** — 新增 `PersistedPlayerData` 接口（filePaths/lastFolder/autoDecrypt/lastIndex/playbackTime）+ `DEFAULT_PLAYER_DATA` 默认值
2. **stores/settings.ts** — 新增 `playerData` ref、`loadPlayerData()` 加载函数、`setPlayerData()` 写回函数、`watch(deep)` → localStorage 自动持久化（key: `snve-player-data`）
3. **PlayerView.vue** — 核心集成：
   - `onMounted` 从 store 恢复 playlist/fileIndex/autoDecrypt/lastFolder
   - `watch([files.paths, currentIndex, autoDecrypt])` 自动 saveToStore
   - `selectDir()` 后保存 `lastFolder.value`
   - 新增 `rescanLastFolder()` 一键重新扫描上次文件夹
   - `initAndPlay` 中 Plyr `canplay` 事件恢复 playbackTime
   - `pause`/`ended`/`onUnmounted` 时 `savePlaybackTime()`
   - 引用 `useSettingsStore`
4. **PlaylistPanel.vue** — 新增 `lastFolder` prop + `rescanLastFolder` emit + `getFolderName()` 工具函数
   - 模板新增蓝色虚线"重新加载 {文件夹名}"按钮（仅 lastFolder 非空时显示）

### 不持久化内容
- `tempPath` 临时解密文件（每次会话重新解密）
- `meta` 视频元数据（重新加载，失效路径由 `Promise.allSettled` 静默跳过）
- `passwordInput`/`showPasswordModal` 等瞬态 UI 状态

lint: 零新增错误

## Player 视频截图功能（4 项）

FFmpeg 方案（原分辨率帧提取），输出到视频同目录。

### 修改文件
1. **main/modules/ffmpeg.ts** — 新增 `captureScreenshot()` 函数（spawn `ffmpeg -ss <time> -i <input> -vframes 1 -q:v 2 -y <output>.png`），支持取消 + `ProgressCallback`
2. **main/index.ts** — import `captureScreenshot`，在 `registerPlayerHandlers()` 中注册 `wrapOperation('video:screenshot', 'screenshot', ...)`
3. **preload/index.ts + index.d.ts** — 新增 `captureScreenshot(opts)` API；`ProgressInfo.type` 新增 `'screenshot'`
4. **views/Player/PlayerView.vue** — 完整截图 UI：
   - Header 新增 📷 Camera 按钮（仅当前文件存在时显示）
   - 模态弹窗三 Tab：当前画面、指定时间、批量截图
   - 当前画面：显示当前时间，一键截图
   - 指定时间：支持秒数/分:秒/时:分:秒 三种输入格式
   - 批量：间隔秒数输入 + 帧数预览 + 进度条
   - 加密视频保护：未解密时提示
   - 文件命名：`{name}_screenshot_HH-MM-SS.png` / `{name}_frame_NNN.png`

lint: 零新增错误

## Player 模块代码审查与重构（7 项）

基于 CLAUDE.md 规范的全面审查，修改文件：PlayerView.vue / _player.scss / utils/time.ts。

### 修改详情
1. **BUG 修复** — 补 `X` 图标导入（模板 line 652 使用但未 import，会导致运行时错误）
2. **合并重复代码** — `captureCurrentFrame` + `captureByTime` 80% 重复 → 提取 `doSingleCapture(timeSec)` 统一包装，各函数仅传参调用（-20 行）
3. **移除死状态** — `captureSuccess` ref 从未在 UI 中展示，已删除
4. **工具函数归位** — `hmsToSeconds(input:string)` 移至 `@/utils/time` 重命名为 `parseTimeInput`（遵循 CLAUDE.md "视图组件禁止内联定义工具函数"）
5. **正则预编译** — `EXT_RE` / `COLON_RE` / `DIR_SEP_RE` 提升为模块级常量；`ERR_NO_VIDEO` 提取重复错误字符串
6. **内联样式归位** — `style="max-height: 55vh; min-height: 240px"` → `_player.scss` 新增 `.video-constrain` / `.video-placeholder` 类（遵循设计 Token 原则 #5）
7. **调用合并** — `onMounted` 中 `await addFiles()` + `void loadAllMeta()` → `await addFilesAndLoadMeta()` 一步到位

lint: 零新增错误（仅 pre-existing `@use` CSS 误报）
