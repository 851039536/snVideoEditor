# 2026-06-30

## EncryptView.vue 审查与修复（不涉及加密逻辑）
- **新增** `views/Encrypt/types/index.ts` — 移入 `CryptoEntry` 接口（原则1：功能模块化）
- **新增** `assets/styles/_encrypt.scss` — 提取 `code` 样式（原则6：样式分离）
- **删除** `.input-field` 死 CSS（手动密码输入移除后遗留，记忆 ID 66922502）
- **删除** `CryptoEntry` 内联接口声明
- lint: 仅 1 个 pre-existing `@use` CSS 误报，零新增错误

## Player 模块逻辑审查与修复（共 11 项）

### 内存泄露修复
1. `onUnmounted` → `async` + `await cleanupAllTemps()`，确保组件销毁前临时文件被删除
2. `removeFile` → `async` + `await cleanupTemp()`，先删文件再从列表移除

### 逻辑漏洞修复
3. IIFE init `tempDir` 改为 `tempDirReady` Promise + try-catch，`decryptAndPlay` 内 `await tempDirReady` 确保就绪
4. `.enc` 检测改为 `p.toLowerCase().endsWith('.enc')` 替代 `split('.').pop()`
5. `loadMeta` 增加 `entry.isEncrypted && !entry.tempPath` 防御性守卫
6. 新增 `decrypting` 并发锁，防止快速切换触发多重解密
7. 错误横幅增加 X 关闭按钮

### 冗余消除
8. 移除 `tempPaths: Map<string,string>` — 改为 computed `tempCount` 从 files 派生
9. `cleanupAllTemps` 改为直接遍历 files 而非 Map
10. 移除 `decryptWithDefaultKey()` 一层包装，内联为 `decryptAndPlay(file, DEFAULT_ENCRYPT_KEY)`
11. 模板 `tempPaths.size` → `tempCount`

lint: 零新增错误，1 个 pre-existing `@use` 误报
