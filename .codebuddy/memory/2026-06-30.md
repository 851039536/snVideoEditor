# 2026-06-30

## CompressView 模块扩展改造（8 项改进）

用户要求对 CompressView.vue 执行 8 项改进，全部完成：

### 1. 样式提取为 SCSS partial
- 新建 `src/renderer/src/assets/styles/_compress.scss`（85 行）
- 提取滑块、模态框、过渡动画样式
- CompressView.vue `<style scoped>` 从 97 行缩减为 2 行 `@use`

### 2. 拆分 VideoDetailModal 组件
- 新建 `src/renderer/src/views/Compress/VideoDetailModal.vue`（约 130 行）
- Props: `entry: FileEntry | null`，Emit: `close`
- CompressView.vue 从 600 行缩减至约 500 行

### 3. 压缩预设持久化
- `stores/settings.ts` 新增 `CompressPreset` 接口（7 字段：crfValue/resolution/bitrate/codec/audioBitrate/preset/twoPass）
- `localStorage key: snve-compress-preset`，watch deep 自动回写
- CompressView.vue 参数从 store 初始化，watch 数组自动 savePreset()

### 4. 压缩前后对比展示
- batchCompress 成功后在 finally 中调用 `getFileInfo()` 查询输出文件大小
- 新增 `compressResult` ref + ProgressPanel 下方的对比卡片
- 显示：文件名 | 原始大小 → 压缩后大小 | 节省百分比

### 5. 失败文件明细报告
- errorMsg 显示具体失败文件名列表而非仅数量

### 6. CRF 标签修正
- 原标签: "无损/最佳/默认/低质量"
- 新标签: "0 真无损/18 视觉无损/23 默认/28 标清/51 最差"

### 7. 移除死代码
- 按钮文案 `progressStore.isProcessing ? '压缩中...' : '开始压缩'` → `开始压缩 (N 个文件)`

### 8. preset/2-pass 编码选项
- 新增 preset 下拉（ultrafast ~ veryslow，仅 CPU 编码器时显示）
- 新增 2-pass toggle switch（仅 CPU + bitrate 模式下显示）
- ffmpeg.ts: `CompressOptions` 新增 `preset?` + `twoPass?`
- compressVideo 重构为 `buildCompressArgs()` + `runCompressPass()` 双函数
- 2-pass 编码：pass 1 分析（null 输出）→ pass 2 真实编码，自动清理 ffmpeg2pass-0.log

### 架构审查
- 新建 `views/Compress/types/index.ts`（CompressResultItem 接口）
- universal-arch-skill 审查：6 大原则全部通过

### 修改文件清单（9 个文件）
- **新增**: `assets/styles/_compress.scss`, `Compress/types/index.ts`, `Compress/VideoDetailModal.vue`
- **修改**: `Compress/CompressView.vue`, `stores/settings.ts`, `main/modules/ffmpeg.ts`, `main/index.ts`, `preload/index.ts`, `preload/index.d.ts`

Lint: 0 新增错误（仅 `@use` CSS linter 误报）
