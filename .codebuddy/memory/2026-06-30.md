# 2026-06-30 全项目审查与优化

## 审查阶段
使用 code-explorer 子代理完成全项目源码资产审计（78 个源文件，9500+ 行），识别出 15 类问题涵盖类型安全、安全风险、代码质量和架构四大领域。

## 四阶段优化执行

### Phase 1: Bug 修复 + 依赖清理
- 修复 `progress.ts` start() 中 type 参数未使用 bug
- 合并 `file.ts` scanVideoFiles/scanPlayerFiles → scanFiles() 通用函数
- 📁 emoji → FolderOpen 图标（PlaylistPanel.vue）
- 移除 playwright/naive-ui 依赖
- 移除 lastPassword/setLastPassword 死代码
- 移除 ENCRYPTED_FILTER 未使用常量

### Phase 2: 大文件拆分
- ffmpeg.ts (1262行) → 5 文件（shared + compress + gif + thumbnails + barrel）
- PlayerView → 提取 useScreenshot composable
- 所有模块零 lint error

### Phase 3: 类型统一 + 锁增强
- lock.ts: LockTimeoutError + 30s 超时 + getLockDuration()
- main/index.ts 适配新 API

### Phase 4: 安全加固
- PBKDF2: 10000 → 600000 迭代
- V3 格式: HMAC-SHA256 完整性认证
- 向后兼容 V2

## 结果
- 全项目零 lint error
- 5 个新文件，10 个文件修改
