## 2026-06-27

### 浅色主题支持
- 新增深色/浅色双主题切换功能
- **CSS变量体系**：在 `:root`（深色）和 `.light`（浅色）中定义完整颜色变量，含 HSL 变量和 `--color-*` 变量两套
- **Tailwind 联动**：`tailwind.config.js` 中所有颜色改为 `var(--color-*)`，通过 `.light` class 自动切换
- **settings store**：新增 `theme` ref + `toggleTheme()` 方法，localStorage 持久化，`applyTheme()` 在 init/watch 时自动给 `<html>` 添加/移除 `.light` class
- **SideNav 主题按钮**：底部新增 Sun/Moon 图标切换按钮（深色显示 Sun、浅色显示 Moon）
- 修改文件：`global.scss`、`tailwind.config.js`、`stores/settings.ts`、`components/SideNav.vue`

### views 目录重构
- 所有功能模块移入 views 子目录：`Home/HomeView.vue`、`Compress/CompressView.vue`、`Encrypt/EncryptView.vue`
- `SplitMergeView.vue` + `ClipList.vue` 移入 `views/SplitMerge/`
- `router/index.ts` 全部导入路径同步更新
- 创建 `CODEBUDDY.md` 项目规范文件，更新 `MEMORY.md` 新增文件组织规则

### 修复 SplitMerge 裁剪合并 bug
- **根因1**：切到合并模式时 `files` 数组未清空，原始视频混入合并输入 → `watch(mode)` 新增 `merge` 分支清空 `files.value = []`
- **根因2**：ffmpeg `-ss` 在 `-i` 之后导致 `-c copy` 模式下裁剪不准 → 调整参数顺序为 `-ss startTime -i input -t duration`
- 修改文件：`views/SplitMerge/SplitMergeView.vue`、`main/modules/ffmpeg.ts`

### 5 个逻辑漏洞修复
1. **onProgress 内存泄漏**：preload `onProgress()` 内先 `removeAllListeners` 再注册；CompressView/EncryptView 补充 `onUnmounted(() => removeProgressListener())`
2. **elapsed 不实时**：`progress.ts` 中 elapsed 从 computed 改为 `ref + setInterval` 每秒自增，`finish()/reset()` 中 `clearInterval`
3. **videoMeta 竞态**：`loadVideoMeta()` await 返回后检查 `files[0]` 是否仍为请求时的 filePath，不匹配则丢弃
4. **模式切换残留**：`watch(mode)` 切 merge 时追加清空 `outputName`/`outputDir`/`errorMsg`
5. **temp 文件泄漏**：新增 `file:delete` IPC + preload `deleteFile` API；`removeClip` 时删 temp、merge 成功后遍历删已选 clip 的 temp
- 修改 7 个文件：`preload/index.ts`、`preload/index.d.ts`、`main/index.ts`、`stores/progress.ts`、`views/SplitMerge/SplitMergeView.vue`、`views/Compress/CompressView.vue`、`views/Encrypt/EncryptView.vue`

### 合并输出命名改为 SN 时间戳
- 合并模式输出文件名从固定 `merged_output.mp4` 改为 `SN_YYYYMMDDHHmmss.mp4`
- 新增 `getMergeOutputName()` 辅助函数生成时间戳格式文件名
- 修改文件：`views/SplitMerge/SplitMergeView.vue`

### 全局 UI 缩放至 87.5%
- `global.scss` 在 `@layer base` 中新增 `html { font-size: 14px; }`（原 16px），所有 Tailwind rem 单位自动等比缩小
- `SideNav.vue` navWidth 从 px 改为 rem（`200px`→`12.5rem`，`64px`→`4rem`）
- `SplitMergeView.vue` .time-input 的 `width: 32px`→`2rem`，`font-size: 11px`→`0.6875rem`
- `EncryptView.vue` 和 `CompressView.vue` 删除硬编码 `font-size: 14px`，自动继承 html 基准
- 5 个文件 lint 验证通过，零新增错误

### 移除所有鼠标 hover/focus 动画
- 涉及 6 个文件，移除全部 `hover:`/`group-hover:` Tailwind 变体、`:hover`/`:focus` CSS 规则及配套 `transition-*`
- `global.scss`：移除 `.glass-card:hover` scale(1.02) + transition；`.glow-hover` 整类注释
- `SideNav.vue`：移除 nav-item 渐变 hover 背景伪元素、所有按钮 hover/transition
- `HomeView.vue`：移除 6 处 group-hover 效果（渐变背景/缩放/颜色切换/箭头移动）
- `CompressView.vue`：移除表格行 hover、预置按钮 hover、focus、输出按钮/主按钮 hover shadow
- `EncryptView.vue`：移除模式 Tab hover、文件行 hover、delete 按钮 group-hover、密码按钮 hover、input focus
- `SplitMergeView.vue`：移除播放按钮 hover、裁剪/合并按钮 hover shadow、trim-handle hover before/after 伪元素、time-input focus、文件行/排序/删除按钮 hover
- 6 个文件 lint 零新增错误
