# 2026-06-26

## SN Video Editor 项目初始化
- 从零搭建 electron-vite-vue 视频编辑桌面应用
- 创建 34 个文件，约 2000+ 行代码
- 完成三大核心功能模块：分割合并、视频压缩、视频加密/解密
- 架构：Electron 主进程（FFmpeg + crypto）+ 预加载安全桥接 + Vue3 渲染进程
- 设计：深色科技风，暗色主题 + 霓虹蓝紫渐变 + TailwindCSS
- 依赖：需运行 `npm install` 完成依赖安装（含 ffmpeg-static 二进制）

## 环境修复（Electron + SCSS + 视频播放）
- Electron 二进制未下载：npm allow-scripts 阻止了 install.js，手动执行下载 + 配置 .npmrc 镜像
- sass-embedded 未安装：`npm install -D sass-embedded`
- 视频黑屏：file:// 被 CSP 阻止 → 设置 webSecurity: false
- 标题栏：新增自定义 TitleBar 组件（无边框窗口 min/max/close）
- 时间轴拖拽：把手改用 @mousedown 直接绑 + data-handle 属性检测 + overflow:visible

## 裁剪-合并工作流重构（split-to-list-then-merge）
- 改为"裁剪→入列表→勾选→合并"多步流程
- 新增 ClipItem 接口（id/sourceFile/startSec/endSec/duration/outputFile/selected）
- 裁剪模式：时间轴选段 → "裁切到列表"按钮 → ffmpeg -c copy 提取到临时目录 → 加入 clips 列表
- 合并模式：clips 勾选列表 + 外部文件拖放 + 排序移除 + "合并选中"按钮
- 主进程新增 app:getTempDir IPC（返回系统 temp/sn-video-clips 目录）
- preload 新增 getTempDir() API 及类型声明
- 合并时先收集选中 clips 的 outputFile，再拼接外部文件，统一 via concat demuxer
