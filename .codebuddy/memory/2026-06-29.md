# 2026-06-29

## Codex UI Style Guide 样式审查 + 修复

- 使用 codex-ui-style-guide 技能对 snVideoEditor 项目进行第二次样式审查
- 覆盖 15 个文件（1 个 SCSS + 14 个 Vue 组件/视图）
- 前次报告（2026-06-26）80 处违规 → 本次 56 处违规（↓30%）
- P0 级违规全部修复（0 个，前次 14 个）
- 关键成就：darkMode 配置补充、glass-card/glow-hover 的 box-shadow P0 全部移除、多个组件色值改用 CSS 变量
- 3 个新增文件首次纳入审查：GifConvertView(14处)、PlayerView(5处)、DownloadQueue(2处)
- 报告已写入 docs/codex-style-review-report.md

## 修复执行（同日）

基于审查报告对 10 个文件执行了 P1 违规修复（49/61 项修复，12 项保留为可接受/微 px）：

- **新增 CSS 变量**：`--color-playhead`（播放头红）、`--font-size-base`（14px），在 :root 和 .light 中均定义
- **global.scss**：15/17 项修复。transition 统一 0.12s，padding px→rem，border-radius→var()，trim-handle/slider 尺寸 rem 化
- **SideNav.vue**（JS）：7/7 项修复。navItems 颜色从硬编码 hex→CSS 变量引用（`var(--color-*)`）
- **SplitMergeView.vue**：5/8 项修复。timeline-dimmed RGBA→hsl()，border-right/playhead hex→var()，video 内联 style→Tailwind class
- **GifConvertView.vue**：7/11 项修复。timeline 色值→var()，dimmed RGBA→hsl()，video 内联 style→Tailwind class
- **EncryptView.vue**：9/9 项修复。passwordStrength 颜色→var()，input-field padding px→rem，code fontSize/padding→rem，video 内联 style→Tailwind
- **PlayerView.vue**：3/4 项修复。input-field padding→rem，slider range/thumb px→rem
- **ProgressPanel.vue**：1/1 修复。transition 0.2s→0.12s
- **TitleBar.vue**：1/1 修复。关闭按钮 #E81123→var(--color-danger)
- **App.vue**：1/1 修复。page transition 0.3s→0.12s
- 保留项：微 px 值（scrollbar 4px/2px、playhead 2px）、P2 装饰渐变/box-shadow、font-weight 600（对齐 Tailwind semibold）
- 验证：ESLint 0 新增错误，仅预存 @/ 别名和 SCSS @tailwind 警告
- 报告已更新修复记录至 docs/codex-style-review-report.md

## 修复 @/ 路径别名 ts-plugin 错误

- 所有 renderer .vue 文件都报 `找不到模块"@/..."` ts-plugin 错误
- 根因：tsconfig.web.json 缺少 `paths` 映射 + `include` 中 `src/preload/*.d.ts` + `composite: true` 冲突
- 修复 1：tsconfig.web.json 添加 `baseUrl: "."` + `paths: { "@/*": ["./src/renderer/src/*"] }`
- 修复 2：从 include 移除 `src/preload/*.d.ts`，改为在 env.d.ts 中添加 `/// <reference path="../../preload/index.d.ts" />` 引入预加载类型
- 修复 3：删除过期的 tsconfig.web.tsbuildinfo 和 tsconfig.node.tsbuildinfo 缓存
- 结果：CompressView.vue 的 4 个 @/ 路径错误全部消除

## 播放器 UI 重构与新功能（2026-06-29）

将 PlayerView.vue（782行单文件）拆分为 3 组件 + 1 SCSS + 1 types 文件：

### 新增文件
- `Player/types/index.ts`：PlayerEntry 接口、PlaybackSpeed 类型、PLAYBACK_SPEEDS 常量、formatSpeed() 格式化
- `Player/_player.scss`：共享 SCSS partial（进度条定制、控件覆盖层动画、缓冲预览、倍速菜单、拖拽排序、全屏覆盖层、响应式），所有颜色使用 CSS 变量
- `Player/PlaylistPanel.vue`：播放列表子组件，含 FileDropZone + 扫描文件夹 + 文件列表 + HTML5 拖拽排序（dragstart/dragover/drop）+ 悬浮删除 + 清空
- `Player/VideoControlsBar.vue`：控制条子组件，含自定义进度条（mousedown拖拽+缓冲预览）、音量滑块(0-1 range)、6档倍速菜单(0.5x-2.0x)、全屏按钮、播放/暂停/上/下曲

### PlayerView.vue 新增功能
- **音量滑块**：volume ref(0-1) + video.volume + onVolumeChange，滑块自动取消 mute
- **倍速播放**：playbackSpeed ref(0.5/0.75/1.0/1.25/1.5/2.0) + video.playbackRate
- **进度条拖拽**：mousedown→mousemove→mouseup 实时拖拽 + 缓冲预览（video.buffered TimeRanges）
- **全屏**：videoContainer.requestFullscreen() + document.exitFullscreen() + fullscreenchange 事件监听
- **居中播放覆盖层**：暂停时显示 56px 半透明圆环大播放按钮，点击视频画面播放/暂停
- **控制条自动隐藏**：播放时 3s 无鼠标移动自动淡出，暂停或鼠标移动时显示
- **拖拽排序**：handleReorder() 处理 from→to splice 移动，同步更新 currentIndex

### 技术要点
- 无 IPC/主进程变更，所有新功能纯前端
- 样式全部提取至 _player.scss，PlayerView.vue `<style>` 仅一行 `@use "./_player"`
- 保留所有现有业务逻辑（加密解密、自动解密、密码弹窗、视频信息卡片）
- Lint：0 错误，仅 1 个 CSS @use 误报警告

## Plyr 替换自定义播放器控件（2026-06-29 同天）

用 Plyr（~15KB gzipped）替换了所有自定义播放控件，消除手写控件的边界问题：

### 变更文件
- **删除**：`Player/VideoControlsBar.vue`（185行自定义控件全部移除）
- **PlayerView.vue**：506行（原682行，↓26%）。移除了15个ref（isPlaying/currentTime/duration/isMuted/volume/playbackSpeed/isFullscreen/showControls/bufferedPercent等）、8个事件处理函数、3个控件函数。新增 Plyr init（30行）+ 2个 watch
- **_player.scss**：230行→138行（↓40%）。删除自定义进度条/控件叠加层/音量滑块/倍速菜单/全屏 CSS，新增 Plyr CSS 变量主题覆盖
- **types/index.ts**：17行→8行。删除 PlaybackSpeed 类型/PLAYBACK_SPEEDS 常量/formatSpeed 函数

### Plyr 提供的能力（替代手写代码）
| 功能 | 原手写 | Plyr |
|------|--------|------|
| 进度条拖拽+缓冲 | ~40行 mousedown/move/up | 内置 |
| 音量滑块 | ~20行 + volume-slider CSS | 内置 |
| 倍速选择 | ~30行 dropdown 代码 | 内置 settings 菜单 |
| 全屏 | ~15行 + fullscreenchange 监听 | 内置 |
| 居中播放按钮 | ~15行 overlay HTML+CSS | 内置 play-large |
| 控制条自动隐藏 | ~20行 timer+showControls | 内置 hideControls |
| 键盘快捷键 | 无 | 内置 ←→↑↓MF空格 |
| 画中画 | 无 | 内置 PIP |
| ARIA 可访问性 | 无 | 内置 |

### 保留不改
- PlaylistPanel.vue（播放列表+拖拽排序）→ 完全不变
- 加密解密逻辑、密码弹窗、自动解密开关、视频信息卡片 → 完全不变
- IPC/preload → 零变更
- 文件管理（addFiles/removeFile/reorder/clearList/scanDir）→ 完全不变

## Player 模块冗余清理（2026-06-29）

8 处冗余修复，提取 3 个共享 helper，代码减少约 50 行：

### 提取的 helper
- **destroyPlayer()**：统一 player destroy + isPlaying=false，替换 initAndPlay/clearList/onUnmounted 3 处重复
- **decryptAndPlay(file, password)**：统一解密流程（清理旧 tempPath→decryptForPlayback→检查切走→记录 tempPath→loadMeta→initAndPlay），decryptWithDefaultKey 和 confirmDecrypt 改为薄 wrapper。内部复用 `tempDir.value` 消除 2 次重复 IPC 调用
- **clearList()**：直接调用 destroyPlayer() + cleanupAllTemps() 替代手写遍历清理

### 其他修复
- loadAllMeta 调用加 `void` 前缀（fire-and-forget 显式标记）
- 模板 `relative bg-black` div 缩进 +2（正确反映父子关系）
- _player.scss 删除空注释块 `// ---- Plyr Dark Tech Theme Overrides ----`
- initAndPlay 过时注释自动随代码替换清除

### lint 结果
0 新增错误，仅预存 CSS @use 误报 + FileDropZone ts-plugin 类型误报

## CompressView 代码审查与修复（2026-06-29）

审查发现 5 个问题并全部修复：

### 修复 1：audioBitrate 类型链补齐（功能性 Bug）
- 前端已发送 `audioBitrate`，但 `BatchCompressOptions.files` 类型链的 4 处缺少该字段
- 修改：`ffmpeg.ts` / `main/index.ts` / `preload/index.ts` / `preload/index.d.ts` 各加 `audioBitrate?: string`
- 主进程 `batchCompress` 用 `...file` 展开传给 `compressVideo`，值自动流通无需额外逻辑

### 修复 2：onProgress 监听器泄露
- `startCompress()` 成功/异常后均未移除监听器，仅依赖 `onUnmounted`
- 修复：在 try-catch 后添加 `finally { window.electronAPI.removeProgressListener() }`

### 修复 3：selectedOutputDir 冗余状态
- 删除 `selectedOutputDir` ref + watcher（60-64行），改为 computed 从 `files[0]?.outputPath` 派生
- 删除 `selectQuickDir` 中的 `selectedOutputDir.value = dir` 手动赋值（已被 setOutputDir 覆盖）

### 修复 4：getOutputDir/sourceDir 逻辑重复
- 在 `format.ts` 新增 `getDirName(path)` 共享工具函数
- `sourceDir` computed 和 `selectedOutputDir` computed 改用 `getDirName`
- 删除 CompressView 中的 `getOutputDir` 函数

### 修复 5：getAvailableEncoders 竞态风险
- 新增 `let isUnmounted = false` 标志位
- `onMounted` 中 encoders 赋值前检查 `!isUnmounted`
- `onUnmounted` 设置 `isUnmounted = true`

### 修改文件清单
`src/renderer/src/utils/format.ts`（+getDirName）、`src/main/modules/ffmpeg.ts`（+audioBitrate 类型）、`src/main/index.ts`（+audioBitrate 类型）、`src/preload/index.ts`（+audioBitrate 类型）、`src/preload/index.d.ts`（+audioBitrate 类型）、`src/renderer/src/views/Compress/CompressView.vue`（4 处运行时修复）。Lint 0 新增错误。

## CompressView 视频详情弹窗（2026-06-29）

为 CompressView 文件列表每行增加「详情」按钮（Info 图标），点击弹出居中玻璃态弹窗展示视频详细信息：

- **模板**：Teleport to body + Transition 动画（modal-fade scale+opacity），遮罩层点击/ESC 关闭
- **显示内容**：文件路径、视频编码、分辨率（宽×高）、时长（HH:MM:SS）、文件大小、视频码率（Kbps）
- **数据获取**：优先使用已有 `entry.meta`，若无则调用 `getVideoMeta` IPC 懒加载（含 `isUnmounted` 守卫防竞态）
- **样式**：CSS 变量 Token（`--z-modal`/`--transition-normal`/`--radius-sm` 等），`.detail-item` 使用 bg-secondary 背景色卡片式布局
- **修改文件**：仅 `src/renderer/src/views/Compress/CompressView.vue`（+Info 图标导入、+getFileName/secondsToHMS 工具导入、+详情弹窗状态/函数/模板/样式约 100 行）。Lint 0 错误。
