## 2026-06-28

### GifConvertView.vue 代码审查与优化

**P0 - Bug 修复：**
- `syncTrimFromInputs` 添加 try-finally 保护，防止 syncing 标志位永久锁死
- 全局事件监听器（mousemove/mouseup）从模块顶层迁移到 `onMounted`，修复 Vite HMR 内存泄漏
- 统一拖拽最小间隔为 `MIN_TRIM_GAP = 0.1`（原来输入框用 0.5，手柄用 0.1）

**P1 - 去冗余：**
- 删除死 CSS：`.preset-btn` 空规则、`.input-field`、`.fade-*` 过渡类
- 【新文件】`src/renderer/src/utils/time.ts` — secondsToHMS / hmsToSeconds / formatDuration
- 【新文件】`src/renderer/src/utils/math.ts` — clamp
- 【新文件】`src/renderer/src/types/file.ts` — 共享 FileEntry 接口
- GifConvertView 和 CompressView 均改用共享工具函数和类型

**P2 - 架构优化：**
- 【新文件】`src/renderer/src/composables/useFileList.ts` — 提取 addFiles/getMeta/removeFile/selectOutputDir 为可复用 composable
- 两个视图均改用 useFileList()，selectOutputDir 接受 suffix 参数
- `.select-input` 样式提取到 `global.scss` 的 `@layer components` 中
- 同步清理 CompressView 的空 `.preset-btn` 规则

**扩展建议（记录待后续评估）：**
- 自定义循环次数输入
- GIF 生成后预览
- batch 失败详情（列出失败文件名）
- timeline 帧级精度
- 全局快捷键（空格/I/O）
